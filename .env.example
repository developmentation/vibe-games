# Claude Application Harness
#
# This harness does not require any credentials by default. The lifecycle
# phases operate purely on local files under ./phases/ and ./app/. Add
# project-specific environment variables here if your application needs them.
#
# Example: a target-app server reads its own variables from app/.env, not this file.

## Security - Used for Red Blue agent probablistic analysis
ANTHROPIC_API_KEY=